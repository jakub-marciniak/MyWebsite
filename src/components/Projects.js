import React from 'react';
import styled from 'styled-components';

const ProjectsWrapper = styled.section`
    
`;

const Projects = () => {
    return(
        <ProjectsWrapper className="projects" id="projects">
            <div></div>
        </ProjectsWrapper>
    )
}

export default Projects;
